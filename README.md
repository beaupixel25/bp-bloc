# bp-riverpod

A develpoment agency

A Flutter monorepo: Melos workspace, Clean Architecture, a shared `core`
package, and one flavored app package per app.

- **core** — shared domain, DI plumbing, theme/design-system, components
- **hello** — app package

## Getting Started

Initialize the project after it is scaffolded:

```bash
melos bs            # Bootstrap the workspace
melos pub-get       # Get dependencies for all packages
melos build         # Run code generation in all packages
```

Then run an app:

```bash
melos hello   # Prompts for flavor and device
```

## Architecture at a glance

| Concern | This workspace |
|---|---|
| Workspace | Melos 8 + Dart pub workspace. Every package lives in `packages/`; there is no `melos.yaml` (Melos 6+ reads the `melos:` section of the root `pubspec.yaml`). |
| Application architecture | Clean Architecture — `domain` / `data` / `presentation`, one folder per feature. |
| Shared code | `packages/core` — bootstrap, error handling, theme/design-system, reusable components, base classes. Every app package depends on it. |
| State management | flutter_bloc — one `Bloc` per page, sealed events and states |
| Dependency injection | `get_it` + `injectable`. Bindings are annotations; the graph is generated into `lib/inject.config.dart` |
| Routing | `go_router` with typed routes (`@TypedGoRoute` → `routes.g.dart`). |
| Flavors | `development` / `staging` / `production`, one entrypoint each, plus `main_test.dart`. |
| Code generation | `build_runner`: `freezed`, `json_serializable`, `go_router_builder`, `injectable_generator` |
| `core` exports | `full` — a single `core.dart` barrel re-exporting everything under `src/` |

State management is a **monorepo-level** property: `packages/core` itself is
built around it (the `bootstrap()` signature, the observer, the error-handling
helper, the environment enum), so every app package in this workspace uses the
same setup.

## Folder structure

```
bp-riverpod/
├── pubspec.yaml                    # Workspace root: `workspace:` package list + `melos:` scripts
├── analysis_options.yaml           # Workspace lints
├── CLAUDE.md                       # Rules + required checklists — auto-loaded by Claude Code
├── MEMORY.md                       # This app's current state — imported by CLAUDE.md
├── .github/workflows/              # CI: analyze, test
├── .claude/
│   ├── memory/CHANGELOG.md         # Append-only archive of what MEMORY.md has trimmed
│   └── skills/                     # Claude Code skills shipped INTO this workspace
│       ├── implement-feature/      #   end-to-end Clean Architecture feature
│       ├── create-component/       #   new reusable component in `core`
│       ├── import-design-system/   #   apply a design-system bundle to the core theme
│       └── implement-prototype/    #   build a slice of a Claude Design prototype
└── packages/
    ├── core/                       # Shared, app-agnostic code — every app depends on it
    │   ├── lib/core.dart                       # Public barrel — the whole of src/, one import
    │   ├── lib/src/
    │   │   ├── bootstrap.dart                  # bootstrap() — global error net, DI setup, runApp
    │   │   ├── app_bloc_observer.dart          # AppBlocObserver — bloc errors -> ErrorReporter
    │   │   ├── constants/di_environments.dart  # Environment enum + injectable environment constants
    │   │   ├── build_configuration.dart        # BuildConfiguration (freezed) — per-flavor settings
    │   │   ├── data/repositories/base_repository.dart    # maps raw errors -> AppException
    │   │   ├── data/services/api_client.dart             # HTTP status + backend code -> AppException
    │   │   ├── domain/exceptions/app_exception.dart      # the app's exception vocabulary
    │   │   ├── domain/use_cases/use_case.dart            # UseCase base class
    │   │   ├── error/error_reporter.dart                 # vendor-agnostic crash sink
    │   │   ├── presentation/bloc/              # handleBlocAction + status enums
    │   │   ├── presentation/components/        # Widget + ThemeData + Theme + ViewModel components
    │   │   ├── presentation/error/             # AppException -> user-facing message
    │   │   ├── presentation/theme/             # palette, ColorScheme, ColorExtension, AppThemeData
    │   │   └── presentation/views/apps/app/    # App + BaseApp (MaterialApp.router)
    │   └── demo/                   # core_demo — showcases `core` in isolation (`melos core:demo`)
    └── hello/                      # App package (one per app; generated by Very Good CLI, flavored)
        ├── lib/main_development.dart           # Entrypoint, one per flavor
        ├── lib/main_staging.dart
        ├── lib/main_production.dart
        ├── lib/main_test.dart                  # Runs the app against the `test` environment
        ├── lib/inject.dart                     # COMPOSITION ROOT — GetIt + configureInjection()
        ├── lib/inject.config.dart              # generated by injectable_generator (do not edit)
        ├── lib/app/bloc/app_bloc.dart          # APP-WIDE STATE — @injectable Bloc: startup + theme mode + locale
        ├── lib/app/view/app.dart               # Root widget — builds GoRouter, provides the app-wide state, wraps core's App
        ├── lib/routing/routes.dart             # Typed go_router routes -> routes.g.dart
        ├── lib/common/error/                   # App-specific AppException -> message mapping
        ├── lib/l10n/                           # ARB files + generated localizations
        ├── lib/features/<feature>/             # One folder per feature — see below
        └── test/                               # unit + widget tests
```

### Inside a feature

```
features/onboarding/
├── domain/                   # Layer 1 — pure Dart, no Flutter, no IO, no JSON
│   ├── entities/user.dart              #   User — identity that outlives a single use case
│   ├── dtos/credentials.dart           #   log-in input
│   ├── dtos/email_signup.dart          #   sign-up input
│   ├── dtos/auth_token.dart            #   use-case output
│   ├── repositories/onboarding_repository.dart   # IOnboardingRepository — the seam
│   └── use_cases/login_uc.dart         #   LoginUseCase, SignupUseCase
├── data/                     # Layer 2 — implements the domain's interfaces
│   ├── models/user_model.dart          #   adds fromJson/toJson on top of the domain type
│   └── repositories/
│       ├── onboarding_repository.dart        # real implementation
│       └── mock/onboarding_repository.dart   # test implementation
└── presentation/             # Layer 3 — Flutter UI
    ├── state/credential_draft_cubit.dart       # @lazySingleton Cubit
    └── view/pages/login/
        ├── login_page.dart        #   StatefulWidget — BlocBuilder / BlocListener
        └── login_bloc.dart        #   @injectable Bloc, sealed events -> sealed states
```

## Architecture diagram

### 1. The layers — what lives where

The whole running app, top to bottom. Dashed rules separate the layers; a line
crossing one is a call crossing a layer boundary.

Two paths leave `bootstrap()` and they are drawn separately on purpose. The
left one composes the object graph; the right one mounts the widget tree. They
meet only in the sense that the second happens after the first.

```text
  ─ ─ ─ ─ ─ ─ ─   ENTRYPOINT · the app package  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─

    ┌────────────────────────────────────────────────────┐
    │ packages/hello/lib/                                │
    │                                                    │
    │ main_development.dart     main_staging.dart        │
    │ main_production.dart      main_test.dart           │
    │                                                    │
    │ One per flavor. Each does nothing but call         │
    │ bootstrap() with this flavor's Environment.        │
    │ imports package:core/core.dart                     │
    └────────────────────────────────────────────────────┘
            │  bootstrap(() => const HelloApp(),
            │            initializer: () =>
            │              configureInjection(Environment.development))
  ─ ─ ─ ─ ─ │ ─   BOOTSTRAP · packages/core   ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
            ▼
    ┌────────────────────────────────────────────────────────────────┐
    │ bootstrap()                       core/src/bootstrap.dart      │
    │                                                                │
    │ · installs the global error net ───────────────▶ §6            │
    │ · Bloc.observer = AppBlocObserver                              │
    │ · try {                                                        │
    │     WidgetsFlutterBinding.ensureInitialized()                  │
    │     await initializer()                                        │
    │     runApp(await builder())                                    │
    │   } catch → report + render the failure surface                │
    └────────────────────────────────────────────────────────────────┘
            │  await initializer()            runApp(…) mounts the shell ─┐
            │                                                             │
  ─ ─ ─ ─ ─ │ ─   COMPOSITION ROOT · the app package  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │ ─ ─
            ▼                                                             │
    ┌────────────────────────────────┐  ┌────────────────────────────────┐│
    │ lib/inject.dart                │  │ lib/inject.config.dart         ││
    │                                │  │                                ││
    │ · BuildConfiguration for this  │─▶│ GENERATED registry: repos,     ││
    │   flavor (endpoints, title)    │  │ use cases and blocs whose      ││
    │ · injector.init(environment:)  │  │ env: matches the flavor        ││
    └────────────────────────────────┘  └────────────────────────────────┘│
        injector<T>() resolves into every box below                       │
                                                                          │
  ─ ─ ─ ─ ─ ─ ─   APP SHELL · the app package + core  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │ ─ ─
            ┌─────────────────────────────────────────────────────────────┘
            ▼
    ┌────────────────────────────────────┐  ┌──────────────────────────────────┐
    │ HelloApp       app/view/app.dart   │  │ core: App ──▶ BaseApp            │
    │                                    │  │ core/core.dart                   │
    │ BlocProvider<AppBloc>.value(       │─▶│                                  │
    │   injector<AppBloc>()..start())    │  │ AppTheme (AppColorScheme +       │
    │   startup load · theme mode ·      │  │ dimensions)  core/core.dart      │
    │   locale   (APP-WIDE STATE)        │  │ MaterialApp.router · l10n ·      │
    └────────────────────────────────────┘  │ text scaling · breakpoints       │
            │  routerConfig: $appRoutes     └──────────────────────────────────┘
            │
    ┌────────────────────────────────────────────────────────────────────┐
    │ lib/routing/routes.dart   (typed routes ─▶ routes.g.dart)          │
    │ LaunchRoute `/` ─▶ LandingRoute ─▶ LoginRoute · SignupRoute · …    │
    └────────────────────────────────────────────────────────────────────┘
            │
            │
  ─ ─ ─ ─ ─ │ ─   PRESENTATION · features/<f>/presentation  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
            ▼
    ┌──────────────────────────────┐          ┌────────────────────────────────┐
    │ LoginPage (StatefulWidget)   │          │ LoginBloc   (@injectable)      │
    │                              │          │                                │
    │ BlocBuilder renders state    │─events─▶ │ on<LoginSubmitted>             │
    │ BlocListener runs effects    │          │ emit(state.copyWith(           │
    │ core components, from        │◀─state── │   status: BlocStatus.loading)) │
    │ core/core.dart               │          │ handleBlocAction(…)            │
    └──────────────────────────────┘          └────────────────────────────────┘
                                                              │  useCase.execute()
  ─ ─ ─ ─ ─ ─ ─   DOMAIN · features/<f>/domain  ─ ─ ─ ─ ─ ─ ─ │ ─ ─ ─ ─ ─ ─ ─ ─
                                          ┌───────────────────┘
                                          ▼
    ┌──────────────────────┐  ┌────────────────────────┐  ┌────────────────────┐
    │ entities/ · dtos/    │  │ use_cases/             │  │ repositories/      │
    │                      │  │                        │  │                    │
    │ User, AuthToken      │◀─│ LoginUseCase           │─▶│ IOnboarding        │
    │ pure Dart — no       │  │ extends UseCase<P,R>   │  │ Repository         │
    │ Flutter, no JSON     │  │ core/core.dart         │  │ ── THE SEAM ──     │
    └──────────────────────┘  └────────────────────────┘  └────────────────────┘
                                                                    │  implements
                                                                    │
  ─ ─ ─ ─ ─ ─ ─   DATA · features/<f>/data  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │ ─ ─ ─ ─ ─
                                                                    ▼
    ┌──────────────────────────┐    ┌──────────────────────────────────────────┐
    │ models/ UserModel        │    │ repositories/  extends BaseRepository    │
    │                          │    │ core/core.dart                           │
    │ extends User +           │maps│                                          │
    │ @JsonSerializable        │◀─  │ OnboardingRepository      dev/stg/prod   │
    │ fromJson / toJson        │    │ MockOnboardingRepository  env: test      │
    └──────────────────────────┘    │                                          │
                                    │ guard() ─▶ typed AppException   (§6)     │
                                    └──────────────────────────────────────────┘
                                                         │
                                                         │
  ─ ─ ─ ─ ─ ─ ─   EXTERNAL WORLD  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
                                                         ▼
                                  ┌────────────────────────────────────────────┐
                                  │ HTTP API · local storage · crash reporter  │
                                  └────────────────────────────────────────────┘
```

### 2. Build time — what generates what

Nothing in this workspace is hand-rolled from scratch: the CLI scaffolds the
shape, the skills fill it in, and `build_runner` produces the wiring.

```
  intent                     tool                                    result on disk
  ─────────────────────      ───────────────────────────────────     ─────────────────────────────────
  real feature          ──▶  /implement-feature       (skill)   ──▶  features/<feature>/{domain,data,presentation} + fields, pages, routes, DI wiring
  shared widget         ──▶  /create-component        (skill)   ──▶  core/lib/src/presentation/components/<name>/
  branding              ──▶  /import-design-system    (skill)   ──▶  core/lib/src/presentation/theme/*
  design prototype      ──▶  /implement-prototype     (skill)   ──▶  pages/components/features for one prototype slice
  wire it all up        ──▶  melos build (build_runner)         ──▶  *.g.dart, *.freezed.dart, routes.g.dart, inject.config.dart
```

### 3. Runtime — how the app starts

Every numbered step is a real file you can open. The
[startup walkthrough](#startup-walkthrough) below explains each one and tells
you what to change there.

```
  flutter run --flavor development -t lib/main_development.dart
        │
        ▼
  (1) lib/main_development.dart ......................... per-flavor ENTRYPOINT
        │   bootstrap(() => const HelloApp(),
        │             initializer: () => configureInjection(Environment.development))
        ▼
  (2) core: bootstrap() ................................. src/bootstrap.dart
        │   • FlutterError.onError / PlatformDispatcher.onError ──▶ (3) ErrorReporter
        │   • ErrorWidget.builder  • try/catch around composition
        │   • Bloc.observer = (3) AppBlocObserver
        │   • await initializer() ──▶ (4)
        │   • WidgetsFlutterBinding.ensureInitialized() + usePathUrlStrategy()
        │   • runApp(await builder())
        ▼
  (4) lib/inject.dart — configureInjection() ............ APP COMPOSITION ROOT
        │   • injector.reset()
        │   • registerLazySingleton<BuildConfiguration>(<per-flavor config>)
        │   • maps core Environment ──▶ injectable environment name
        │   • injector.init(environment: …) ──▶ (5)
        ▼
  (5) lib/inject.config.dart (GENERATED) ................ every @injectable / @LazySingleton
        │   registers the annotated repositories, use cases and blocs whose
        │   `env:` matches the environment picked in (4)
        ▼
  (6) HelloApp — lib/app/view/app.dart .................. APP ROOT WIDGET
        │   builds the GoRouter from lib/routing/routes.dart
        │   BlocProvider<AppBloc>(injector<AppBloc>()..start()) ──▶ lib/app/bloc/
        │     (APP-WIDE STATE: startup load + theme mode + locale)
        ▼
  (7) core: App ──▶ BaseApp ............................. src/presentation/views/apps/app/
        │   AppTheme + text scaling + MaterialApp.router + localization + breakpoints
        ▼
 (7a) GoRouter resolves `/` ──▶ LaunchRoute ............. routing/routes.dart
        │   LaunchPage composes the brand, reads isSignedIn off (6a), hands
        │   off at the 2950ms sync point to `/landing` (redirects to
        │   `/login`) or `/main`
        ▼
  (8) <Page> (StatefulWidget) ........................... a feature's presentation layer
        │   injector<PageBloc>()  ──▶ (9)
        │   BlocBuilder renders state, BlocListener runs side effects
        ▼
  (9) <Page>Bloc (@injectable) ──▶ UseCase ──▶ IRepository ──▶ network
```

### 4. Inside a feature — how a dependency resolves

```
  LoginPage (StatefulWidget)                                  presentation
    └─ injector<LoginBloc>()               resolved from GetIt in the State
         LoginBloc(loginUseCase)                              (@injectable)
           └─ LoginUseCase(onboardingRepository)          (@injectable)   domain
                └─ IOnboardingRepository ....... the seam (domain contract)
                      ├─ OnboardingRepository ....... @LazySingleton(env: dev/staging/prod)
                      └─ MockOnboardingRepository ... @LazySingleton(env: test)   data
```

The page never imports a concrete repository or use case — its `State` just
calls `injector<LoginBloc>()`. Which implementation backs
`IOnboardingRepository` is decided entirely by the `Environment` passed to
`configureInjection` in step (1).

`BuildConfiguration` is resolved the same way — `lib/app/view/app.dart` reads
`injector<BuildConfiguration>().appTitle`. Note that it cannot be a constructor
dependency of an `@injectable` class: it is registered by hand in `inject.dart`
*before* `injector.init()`, so injectable's codegen never sees it. To inject it
into a repository, provide it from an `@module` with per-environment getters
instead of registering it directly.

### 5. The dependency rule

```
        Presentation  ──────▶  Domain  ◀──────  Data
     (blocs      , widgets)     (entities,        (models,
                             DTOs, use cases,   repository
                             repository IFs)    implementations)
```

- **Domain** depends on nothing but `core`'s base classes. No Flutter, no HTTP,
  no JSON.
- **Data** implements the repository interfaces the domain declares, and maps
  external payloads into domain types.
- **Presentation** depends on domain use cases only. It never imports `data/`.
- `presentation` and `data` never reference each other. The abstract repository
  in `domain/repositories/` is the seam, and `injectable` decides at runtime which implementation satisfies it.

Cross-package: `packages/hello` depends on `packages/core`; `core` never depends
on an app.

### 6. Error handling

#### One failure, end to end

Follow a single real failure: someone signs up with an address that already has
an account, and the backend answers `409` with
`{"error":{"code":"EMAIL_ALREADY_REGISTERED"}}`.

1. **The wire.** `ApiClient._decode` sees a non-2xx status. `_ApiError.from`
   reads the body *defensively* — an error response is the least trustworthy
   thing a server sends — probing `error.code`, then `code`, then `error_code`,
   and falling back to the HTTP status if none is there or the body will not
   parse at all.
2. **The typed throw.** The status picks the *kind*: `409` is not 401/403/404
   and not ≥500, so it becomes an `UnknownException`; a `422` would be a
   `ValidationException`, a `401` an `UnauthorizedException`. The backend's code
   rides along in `AppException.code`, and the developer-facing detail
   (`HTTP 409: …`) in `message`.
3. **The data boundary.** `BaseRepository.guard` catches it, sees it is already
   an `AppException`, and rethrows it unchanged with
   `Error.throwWithStackTrace` so the original trace survives. Transport
   failures (`SocketException`, `TimeoutException`) and an unparseable *success*
   body enter the vocabulary here instead — that split is deliberate, and
   mapping status codes in both places is how two mappings drift apart.
4. **The domain.** `UseCase.execute` rethrows an `AppException` untouched and
   wraps anything else in an `UnknownException`. Only an `AppException` ever
   reaches presentation.
5. **The state holder.** `handleBlocAction` catches `AppException` **and nothing
   else**, records it as state, and breadcrumbs it to `appErrorReporter`.
   Anything that is not an `AppException` keeps travelling — out of the
   callback, out of the frame, into the global net.
6. **The sentence.** `failure.toUserMessage(AppErrorMessages(context.l10n))`
   asks `forCode('EMAIL_ALREADY_REGISTERED')` first, which resolves
   `l10n.errorEmailAlreadyRegistered` — "That email already has an account. Log
   in instead." An uncoded failure, or a code this app does not recognise,
   falls back to the type: `NetworkException` → `errorNetwork`, and so on.
7. **The sink.** Sentry (or Crashlytics, or whatever you wire) sees this as a
   *breadcrumb*, not an issue: the user got a sentence and the app carried on.
   Had nothing caught it, it would have arrived through
   `ErrorReporter.report` as a crash, with its cause and original stack trace.

#### Where it goes

```text
  ─ ─ ─ ─ ─ ─ ─   EXTERNAL WORLD · where a failure starts   ─ ─ ─ ─ ─ ─ ─ ─ ─ ─

    ┌────────────────────────────────────────────────────────────────┐
    │ SocketException · TimeoutException · HttpException ·           │
    │ FormatException · a client error (DioException, …) ·           │
    │ a 401/403/404/5xx response · a bug in your own code            │
    └────────────────────────────────────────────────────────────────┘
            │  returned to ApiClient, or thrown under the repository
            │
  ─ ─ ─ ─ ─ │ ─   DATA · two translation points, split on purpose   ─ ─ ─ ─ ─ ─
            ▼
    ┌──────────────────────────────────────────────────────────────────────┐
    │ ApiClient._decode(response)     core/…/data/services/api_client.dart │
    │                                                                      │
    │ 401            ─▶ UnauthorizedException                              │
    │ 403            ─▶ ForbiddenException                                 │
    │ 404            ─▶ NotFoundException                                  │
    │ 400 · 422      ─▶ ValidationException                                │
    │ >= 500         ─▶ ServerException                                    │
    │ other non-2xx  ─▶ UnknownException                                   │
    │                                                                      │
    │ carrying  code: the backend's own code, else the HTTP status         │
    │           message: 'HTTP <status>: <detail>'   (logs only)           │
    │                                                                      │
    │ A non-2xx status is an EXPECTED failure, so it is typed here.        │
    │ The body is read defensively — an HTML error page, an empty          │
    │ body or a proxy's output all fall back to the status rather          │
    │ than throwing. A 2xx body that will not parse stays a                │
    │ FormatException for the box below; mapping it in both places         │
    │ is how two mappings drift apart.                                     │
    └──────────────────────────────────────────────────────────────────────┘
            │  an AppException already, or a raw transport failure
            ▼
    ┌──────────────────────────────────────────────────────────────────────┐
    │ BaseRepository                          core/core.dart               │
    │                                                                      │
    │ guard(body) {  try { await body() }                                  │
    │                catch (e, st) { Error.throwWithStackTrace(            │
    │                  handleException(e, st), st) } }                     │
    │                                                                      │
    │ handleException()   Socket · Timeout ─▶ NetworkException             │
    │                     Http             ─▶ ServerException              │
    │                     Format           ─▶ ServerException              │
    │                     AppException     ─▶ unchanged                    │
    │                     anything else    ─▶ UnknownException             │
    │                                                                      │
    │ Override it to map your HTTP client: 401 ─▶ Unauthorized,            │
    │ 403 ─▶ Forbidden, 404 ─▶ NotFound, >= 500 ─▶ Server.                 │
    └──────────────────────────────────────────────────────────────────────┘
            │  a typed AppException — cause + original stackTrace kept
            │
  ─ ─ ─ ─ ─ │ ─   DOMAIN · the typed vocabulary   ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─
            ▼
    ┌────────────────────────────────────────┐  ┌──────────────────────────────┐
    │ sealed AppException   core/core.dart   │  │ UseCase.execute()            │
    │                                        │  │ core/core.dart               │
    │ Network · Server · Unauthorized        │  │                              │
    │ Forbidden · NotFound · Validation      │◀─│ try { await call(input) }    │
    │ Cache · Unknown                        │  │ on AppException ─▶ rethrow   │
    │ Displayable  (already localised)       │  │ catch (e, st)   ─▶ throw     │
    │                                        │  │   UnknownException(cause: e, │
    │ {message, code, cause, stackTrace}     │  │     stackTrace: st)          │
    └────────────────────────────────────────┘  └──────────────────────────────┘
    Only an AppException ever reaches presentation.               │
                                                                  │
  ─ ─ ─ ─ ─ ─ ─   PRESENTATION · expected vs unexpected   ─ ─ ─ ─ │ ─ ─ ─ ─ ─ ─
                        ┌───────────────────────────────────────┴─┐
                        ▼                                         ▼
    ┌────────────────────────────────────────────┐  ┌──────────────────────────┐
    │ EXPECTED — becomes state                   │  │ UNEXPECTED               │
    │                                            │  │                          │
    │ handleBlocAction(                          │  │ not an AppException,     │
    │   () => _useCase.execute(input: …),        │  │ or nothing caught it     │
    │   onFailure: (f) => emit(state.copyWith(   │  │                          │
    │     status: BlocStatus.failure,            │  │ It keeps going: out      │
    │     failure: f)));                         │  │ of the callback, out     │
    │                                            │  │ of the frame, into       │
    │ — only an AppException is caught here;     │  │ the global net.          │
    │   anything else propagates out.            │  └──────────────────────────┘
    └────────────────────────────────────────────┘                │
            ▼                                                     │
    ┌────────────────────────────────────────────┐                │
    │ failure.toUserMessage(     core/core.dart  │                │
    │   AppErrorMessages(context.l10n))          │                │
    │                                            │                │
    │ typed exception ─▶ localised string        │                │
    │ ─▶ FormMessage / SnackBar                  │                │
    │                                            │                │
    │ Server · Cache · Unknown show the generic  │                │
    │ message; their text is for logs only.      │                │
    │ Displayable is shown verbatim.             │                │
    └────────────────────────────────────────────┘                │
        THE USER SEES THIS                                        │
                                                                  │
  ─ ─ ─ ─ ─ ─ ─   GLOBAL ERROR NET · core bootstrap()   ─ ─ ─ ─ ─ │ ─ ─ ─ ─ ─ ─
  ┌───────────────────────────────────────────────────────────────┘
  │  ┌──────────────────────────────────────────────────────────────┐
  ├─▶│ FlutterError.onError                                         │──┐
  │  │ build · layout · paint — the framework caught these and is   │  │
  │  │ telling you. ErrorWidget.builder replaces the red screen     │  │
  │  │ with "Something went wrong."                                 │  │
  │  └──────────────────────────────────────────────────────────────┘  │
  │  ┌──────────────────────────────────────────────────────────────┐  │
  ├─▶│ PlatformDispatcher.instance.onError                          │──┤
  │  │ everything the framework did not catch: uncaught async, a    │  │
  │  │ platform channel, anything escaping main. Returns true, so   │  │
  │  │ the engine does not re-throw.                                │  │
  │  └──────────────────────────────────────────────────────────────┘  │
  │  ┌──────────────────────────────────────────────────────────────┐  │
  ├─▶│ AppBlocObserver.onError                                      │──┤
  │  │ an error no event handler turned into state.                 │  │
  │  │ Installed globally as Bloc.observer.                         │  │
  │  └──────────────────────────────────────────────────────────────┘  │
     There is deliberately no error Zone: a custom zone would          │
     intercept in-zone errors before PlatformDispatcher.onError ever   │
     saw them, and given an async body that throws it abandons the     │
     future it returned — which hangs main() rather than reporting.    │
     A composition failure is caught by bootstrap's own try/catch,     │
     which reports it and renders the failure surface.                 │
                                                                       │
     all of them hand the error and its stack trace to:                │
                                                                       │
  ─ ─ ─ ─ ─ ─ ─   REPORTING SINK · leaves your code   ─ ─ ─ ─ ─ ─ ─ ─ ─│─ ─ ─ ─
                    ┌──────────────────────────────────────────────────┘
                    ▼
      ┌────────────────────────────────────────────────────────────┐
      │ ErrorReporter.report(error, stackTrace)   core/core.dart   │
      │                                                            │
      │ vendor-agnostic; core never names a crash service          │
      │ default NoopErrorReporter() discards everything, so a      │
      │ freshly generated project runs with no backend             │
      │                                                            │
      │ an UnauthorizedException also fires onUnauthorized()       │
      │                                                            │
      └────────────────────────────────────────────────────────────┘
                    │  passed to bootstrap(reporter: …) by main_<flavor>.dart
                    │
      ┌────────────────────────────────────────────────────────────┐
      │ THIRD PARTY · Sentry · Crashlytics · Bugsnag · …           │
      │                                                            │
      │ your app writes one class implementing ErrorReporter       │
      │ and passes it in; core has no dependency on it.            │
      └────────────────────────────────────────────────────────────┘
```

#### What each layer may and may not do

| Layer | Its job | What it must never do |
|---|---|---|
| `ApiClient` | status → exception kind, backend code → `AppException.code` | know a sentence, or map transport/parse failures |
| `BaseRepository.guard` | raw error → typed `AppException`, trace preserved | `catch (_)`, or re-map what `ApiClient` already typed |
| `UseCase.execute` | rethrow typed, wrap untyped | swallow anything |
| `handleBlocAction` | typed failure → state + breadcrumb | catch anything that is not an `AppException` |
| `AppErrorMessages` | code → sentence, else type → sentence | show `message` verbatim |
| `ErrorReporter` | crashes as issues, handled failures as breadcrumbs | live in `core` as a vendor SDK |

Four rules follow, and breaking any of them is what turns a reportable crash
into a silent one:

- **Translate once, at the data boundary.** `guard()` is the only place a raw
  error becomes an `AppException`. A bare `catch (_)` anywhere else discards the
  cause and the stack trace the reporter needs.
- **Catch only what you can act on.** `handleBlocAction` deliberately catches
  `AppException` and nothing else, so a genuine bug reaches the global net
  instead of being rendered as "something went wrong".
- **Never show a raw message.** `toUserMessage` decides what a user reads;
  `ServerException`, `CacheException` and `UnknownException` map to the generic
  string on purpose, and `AppException.message` is for logs.
- **One mapper, and it is the localized one.** New copy is a backend code plus
  an ARB entry, never a second `switch` on exception type. A feature-local copy
  layer looks harmless and silently shadows `toUserMessage`, which is how every
  `error*` string in the ARBs can end up unreachable while the app still looks
  correct.

#### Wiring a real sink

`core` never depends on a crash reporter. Implement `ErrorReporter` in your app
and pass it to `bootstrap` — that installs it for the global net *and* for
`appErrorReporter`, which is what the handled lane uses:

```dart
class SentryErrorReporter implements ErrorReporter {
  @override
  Future<void> report(Object error, StackTrace stackTrace) =>
      Sentry.captureException(error, stackTrace: stackTrace);

  @override
  Future<void> reportHandled(AppException error) async =>
      Sentry.addBreadcrumb(Breadcrumb(message: error.toString()));
}
```

Both methods are required. `implements` takes the interface, never the
implementation, so the default body on `reportHandled` does not spare you.

## Startup walkthrough

How the app is configured and initialized, in the order it happens. The numbers
match the runtime diagram above; the last column is where to make a change of
that kind.

| # | Component | File | Role | Change it when… |
|---|---|---|---|---|
| 1 | `main()` | `packages/hello/lib/main_<flavor>.dart` | The only flavor-aware code in the app: picks the `Environment`, names the root widget, hands both to `bootstrap`. One file per flavor, plus `main_test.dart` for `Environment.test`. | you add a flavor, or need a different root widget per flavor |
| 2 | `bootstrap()` | `packages/core/lib/src/bootstrap.dart` | Installs the global error net (`FlutterError.onError`, `PlatformDispatcher.onError`, `ErrorWidget.builder`), sets `Bloc.observer`, awaits the `initializer` (DI setup), initializes the binding and URL strategy, then `runApp`s the root widget — all inside a `try`/`catch` that renders a failure surface if composition throws. | you need a new app-wide startup step, a crash reporter, or an `onUnauthorized` handler |
| 3 | `ErrorReporter` + `AppBlocObserver` | `core/src/error/error_reporter.dart`, `core/src/app_bloc_observer.dart` | `ErrorReporter` is the vendor-agnostic crash sink (`NoopErrorReporter` by default — pass a real one to `bootstrap`). The observer logs bloc transitions, forwards unhandled bloc errors to the reporter, and fires `onUnauthorized` for an `UnauthorizedException`. | you wire Sentry/Crashlytics, or want session expiry to route to sign-in |
| 4 | `configureInjection()` | `packages/hello/lib/inject.dart` | **The app composition root.** Resets `GetIt`, registers the `BuildConfiguration` for this flavor (app title, base URL, default locale), maps the core `Environment` onto an injectable environment name, then calls `injector.init(environment: …)`. | you change per-flavor endpoints, register something by hand, or add an environment |
| 5 | `inject.config.dart` | `packages/hello/lib/inject.config.dart` (generated) | Produced by `injectable_generator` from every `@injectable` / `@LazySingleton` annotation in the package. Registers exactly the bindings whose `env:` matches the environment chosen in step 4 — so tests resolve `MockOnboardingRepository` and real builds resolve the network one, with no call-site branching. **Never edit**; run `melos build`. | never directly — change the annotations and regenerate |
| 6 | `HelloApp` | `packages/hello/lib/app/view/app.dart` | The app's root widget. Owns the `GoRouter` (built in `initState` from the typed routes in `lib/routing/routes.dart`), provides the `AppBloc` (`BlocProvider` + `..start()`), and passes the app title, localization delegates, the theme built from `state.themeMode` and `state.locale` into core's `App`. | you change the initial location, restructure the shell route, or want an app-specific theme |
| 6a | `AppBloc` | `packages/hello/lib/app/bloc/app_bloc.dart` | **App-wide state**, created once by the app root so it outlives every route. `AppStarted` is the app-wide startup load (`BlocStatus status` + `AppException? failure`, routed through `handleBlocAction`) and sets `isSignedIn` from a stubbed `_restoreSession()` (always `false` today), which `LaunchRoute` reads to choose `/landing` or `/main`; `AppThemeChanged` / `AppLocaleChanged` carry the global theme mode and locale, which `SettingsPage` (tab 2 of the signed-in shell) switches live by dispatching `AppThemeChanged` / `AppLocaleChanged` — the latter takes a `Locale?`, and null means "follow the device". | you have data the whole app needs before its first frame, global UI state more than one screen reads, or a real session check to replace `_restoreSession` with |
| 7 | `App` / `BaseApp` | `packages/core/lib/src/presentation/views/apps/app/app.dart` | `App` resolves the `AppThemeData` (falling back to the core design system), supplies `MediaQuery` + text scaling, and hosts app-wide overlays as siblings of the `MaterialApp`. `BaseApp` is the `MaterialApp.router` itself: routing, light/dark theme, localization delegates, responsive breakpoints. | you add a global overlay (toasts, alerts), a locale, or a breakpoint |
| 7a | `LaunchRoute` / `LaunchPage` | `lib/routing/routes.dart`, `lib/app/view/launch_page.dart` | The app's cold start, at `/`. `LaunchRoute` never names a destination — it hands `LaunchPage` an `onResolved` callback, so deleting a feature can never reach it. `LaunchPage` runs the launch sequence's first two stages off one `AnimationController` (0–3400ms): the bento assembly (`bentoIn` → `bentoOut` → `markIn` → `wordmarkIn`), then the hold (`taglineIn` → `taglineOut`). It reads `isSignedIn` off `AppBloc` and calls `onResolved` at the 2950ms sync point, where `surfaceIn` ends — not at the end of the timeline, so the hand-off never plays against a page that is not built yet. Its last frame is the lockup at rest, which is exactly what `/landing` opens on. | you change what a cold start decides between, or restyle the brand-in animation |
| 8 | `<Page>` | `features/<f>/presentation/view/pages/<page>/<page>_page.dart` | A `StatefulWidget` whose `State` resolves its bloc with `injector<PageBloc>()`, renders with `BlocBuilder`, and runs side effects (navigation, snackbars) with `BlocListener`. | you build or restyle a screen |
| 9 | `<Page>Bloc` | `…/<page>/<page>_bloc.dart` | The page's state holder: an `@injectable` `Bloc` turning sealed events into sealed states. Depends on use cases only — never on a repository. Wraps calls with `handleBlocAction` so only an `AppException` becomes a failure state. | you add a user action, or change how a screen's state is derived |

**Where configuration actually lives.** `BuildConfiguration` (freezed, in
`core`) holds the per-flavor settings and is registered as a lazy singleton in
`configureInjection`, so anything that needs the base URL just resolves
`injector<BuildConfiguration>()`. The `Environment` passed from
`main_<flavor>.dart` is the single switch that decides both the configuration
values and which repository implementations get registered.

**The cold start is a decision, not a fixed hand-off.** `/` always resolves to
`LaunchRoute` first; `/landing` and `/main` are both reachable, but neither is
where the app starts. Signed out lands on `/landing` (`LandingRoute`, inside
`features/onboarding/`), signed in lands on `/main` (`HomeRoute`, app-level,
hosting `HomePage`). `/landing` is stage three of the same sequence, on a
timeline of its own (0–1450ms, starting fresh on every build, not a
continuation of the launch's): the ambient wash settles out of a 1.05 scale,
then the sheet lifts 26px off the bottom edge with its five rows staggering in
behind it. The brand lockup is the one thing that does *not* animate — it
arrives already at rest, in the position the launch left it.

## Implementing a new feature

**The short answer: run the `implement-feature` skill.** It interviews you for
the feature's entities, DTOs, use cases, repository API and screens, writes the
baseline slice, edits all three layers, generates extra pages and components,
wires `go_router`, and runs the build.

```
/implement-feature
```

The same steps in order, and what each one is for:

1. **Generate the slice** — the `/implement-feature` skill writes
   `domain/` (entity, DTO, repository interface, use cases), `data/`
   (model, real + mock repository) and `presentation/` (page + bloc), and adds
   any missing codegen deps to the app's `pubspec.yaml`.
2. **Shape the domain** — edit the entity/DTO fields and the repository
   interface first. Everything else follows from the contract.
3. **Implement the data layer** — the real repository, annotated
   `@LazySingleton(as: I<Feature>Repository, env: [dev, staging, prod])`, and
   keep the `mock/` counterpart (`env: [test]`) in sync.
4. **Build the UI** — page + bloc. The bloc is `@injectable` and depends on use
   cases only; the page resolves it with `injector<…>()`.
5. **Wire routing** — create `presentation/routing/<feature>_routes.dart` with
   a `@TypedGoRoute` class per page and a `List<RouteBase> <feature>Routes`
   collecting them, then spread that list into `appRoutes` in
   `lib/routing/routes.dart`.
   `lib/app/view/app.dart` is never touched — it reads `appRoutes` and never
   names an individual route.
6. **Regenerate and check** — `melos build` (this is what produces
   `inject.config.dart`, so a new `@injectable` does not exist until you run
   it), then `melos analyze`.

Where each step lands in the diagram: steps 1–3 are the *domain* and *data*
layers of "Inside a feature"; step 3 is the binding half of node (5) of the runtime
diagram; the UI steps are nodes (8) and (9).

### Other common changes

| I want to… | Do this |
|---|---|
| Add a reusable widget | `/create-component` — scaffolds the Widget + `ThemeData` (+ `ViewModel`) quartet into `core` and exports it |
| Re-brand the app | `/import-design-system` — applies a design-system bundle to the core theme files |
| Build a screen from a design prototype | `/implement-prototype` — implements one slice of a Claude Design prototype on this architecture |
| Give one app its own colours | pass `theme: AppThemeData.fallback(context, lightColorScheme: AppColorScheme.light(…))` to `App` in that app's `lib/app/view/app.dart` |
| Re-brand every app | edit the semantic aliases in `core/lib/src/presentation/theme/color_palette.dart`, then `melos build:core` |

The complete required-file list for a feature — including the wiring that lives
outside the feature folder — is in `CLAUDE.md`, under
`## What a feature requires`.

## Working with Claude Code

This workspace ships its own Claude Code skills under `.claude/skills/`. They
are the intended entry points for changes — each one already knows this
architecture, so using them is what keeps generated code consistent with the
code that is already here.

| Skill | Use it to |
|---|---|
| `implement-feature` | Build a Clean Architecture feature end to end: interview → baseline slice → all three layers → extra pages and components → routing → build |
| `create-component` | Add a reusable presentation component to `core` (Widget + freezed `ThemeData` + `Theme` + optional `ViewModel`), wire the barrel export, run codegen |
| `import-design-system` | Apply a design-system bundle (colours, typography, dimensions, components) to the `core` theme files |
| `implement-prototype` | Implement or update one slice — a page, flow or feature — of a Claude Design prototype on this architecture |

Three files carry the agent-facing context, and none of them repeat each other:

| File | What it holds |
|---|---|
| `CLAUDE.md` | The enforced rules — the dependency rule, the BLoC + injectable rules, and the required-file checklists for a feature and for a component. Claude Code loads it automatically. |
| `MEMORY.md` | What this app currently *is*: features, components, design system, decisions and gotchas. Imported by `CLAUDE.md`, and updated by each skill as its final step. |
| `.claude/memory/CHANGELOG.md` | Append-only archive of everything trimmed out of `MEMORY.md`, so history is never lost when memory is compacted. |

This README is the remaining half: the diagrams, the walkthrough, and the
reasoning behind the rules.

## Melos Scripts

All scripts run from the project root.

### Workspace

| Script | What it does |
|---|---|
| `melos bs` | Bootstrap the workspace (built-in) |
| `melos pub-get` | `flutter pub get` in every package |
| `melos analyze` | `flutter analyze` in every package |
| `melos build` | pub get + code generation (build_runner) in every package |
| `melos build-watch` | Code generation in watch mode |
| `melos clean` | `flutter clean` in every package |
| `melos test:unit-widget` | All tests with coverage, randomized ordering |

### Core

| Script | What it does |
|---|---|
| `melos build:core` | Build core with code generation |
| `melos clean-build:core` | Clean, then build core with code generation |
| `melos build:core-demo` | Build core, then resolve the demo app |
| `melos clean-build:core-demo` | Same, from a clean slate |
| `melos core:demo` | Launch the core demo app |

### Apps

| Script | What it does |
|---|---|
| `melos build:hello` | Build core, then hello with code generation |
| `melos clean-build:hello` | Same, from a clean slate |
| `melos hello` | Run hello (prompts for flavor and device) |
| `melos hello:development` | Run hello (development flavor, prompts for device) |
| `melos hello:test` | Run hello (development flavor, lib/main_test.dart, prompts for device) |
| `melos hello:staging` | Run hello (staging flavor, prompts for device) |
| `melos hello:production` | Run hello (production flavor, prompts for device) |
| `melos hello:web-server` | Serve hello headlessly on `web-server` (lib/main_development.dart, port `$WEB_PORT`, default 8087) |

## Flavors

Each app supports multiple flavors (build variants):
- **development** — Local dev backend
- **staging** — Staging backend
- **production** — Production backend

Run with: `flutter run --flavor staging --target lib/main_staging.dart`

## Architecture reference

### Layers

Dependencies point **inward** — outer layers depend on inner layers, never the
reverse. See [The dependency rule](#5-the-dependency-rule) for the diagram.

- **Domain** — the innermost layer. Pure Dart with no Flutter, no HTTP, no
  JSON. Holds entities, DTOs, repository *interfaces*, and use cases. It knows
  nothing about how data is fetched or how it is displayed.
- **Data** — implements the repository interfaces declared in domain. Talks to
  external systems (APIs, storage) and maps their payloads into domain types
  via models.
- **Presentation** — Flutter widgets driven by blocs. Depends on domain
  use cases only; it never imports the data layer directly.

### State management (BLoC)

Each screen has a BLoC that turns **events** into **states**. Events and states
are `sealed`/`final` classes so the compiler enforces exhaustive handling. A
BLoC depends only on use cases — never on repositories directly:

```dart
final class LoginSubmitted extends LoginEvent { ... }   // event: user acted

sealed class LoginState { ... }
final class LoginInitial    extends LoginState {}        // idle
final class LoginInProgress extends LoginState {}        // request in flight
final class LoginSuccess    extends LoginState { ... }   // carries AuthToken
final class LoginFailure    extends LoginState { ... }   // carries message
```

The UI rebuilds by reacting to the emitted state — `BlocBuilder` for rendering,
`BlocListener` for side effects (navigation, snackbars).

### Dependency injection (injectable + GetIt)

Wiring is generated by `injectable` and resolved at runtime by `GetIt`. There
is no manual construction and no service locator sprawl in feature code:

```dart
@LazySingleton(
  as: IOnboardingRepository,
  env: [Environment.dev, 'staging', Environment.prod],
)
class OnboardingRepository implements IOnboardingRepository { ... }
```

- **Repositories** are annotated on the *implementation*, bound to the abstract
  interface via `as:`, and scoped by environment via `env:`. A parallel
  `data/repositories/mock/` implementation is registered only for the `test`
  environment, so tests resolve the mock and real builds resolve the network
  implementation — with no call-site branching.
- **Use cases** and **blocs** are `@injectable`; GetIt resolves their
  constructor dependencies automatically.

Run `melos build` after adding or changing an injectable to regenerate
`inject.config.dart` — until you do, the new binding does not exist.

### Entities, DTOs, and Models

The domain and data layers use distinct types on purpose:

- **Entity** — a domain object with identity that persists across use cases
  (e.g. `User`). Pure Dart, no serialization.
- **DTO** — a data-transfer shape crossing a use-case boundary: the input to an
  operation (`EmailSignup`) or its result (`AuthToken`). Also pure Dart.
- **Model** — a data-layer type that adds JSON serialization
  (`fromJson`/`toJson`) on top of an entity or DTO. Models live in `data/` and
  keep serialization concerns out of the domain.

### Model styles: `extends` vs `freezed`

There are two ways to shape a model relative to its domain type:

- **`extends`** (default) — the model is a plain class that `extends` the domain
  type and adds `@JsonSerializable` + `fromJson`/`toJson` (e.g.
  `UserModel extends User`). Because the model *is* an instance of the domain
  type, a repository method typed to return the entity/DTO can return the model
  directly.
- **`freezed`** — the entity is a `@freezed` class implementing a shared
  `<Entity>IF` interface, and the model is a **separate** `@freezed` class
  implementing the same interface plus a `toEntity()` mapper. This decouples the
  JSON/data shape from the domain shape and gives you generated
  `==`/`copyWith`/pattern matching.

### Use Cases vs Services

The application is organized around **Use Cases**. The guiding rule:

> Use Cases orchestrate. Services perform work.

**Use Cases** express business intent — a complete operation initiated by a
user, API request, scheduled job, or external system. Ask *"What is the user
trying to accomplish?"* If the answer is Register, Login, Checkout, ShipOrder,
RefundPayment → create a Use Case. A Use Case may validate requests, load
entities, call repositories and services, coordinate business rules, manage
transactions, publish events, and return a response. It should stay small and
should **not** hold reusable logic that could be shared elsewhere.

**Services** encapsulate reusable capabilities. If the answer is Calculate,
Validate, Convert, Hash, Encrypt, Send, Store, Generate → create a Service.
There are three kinds:

- **Domain Services** — reusable business rules that don't belong to a single
  entity (`PricingService`, `TaxCalculator`, `FraudDetectionService`). They
  contain business logic but do **not** orchestrate repositories or workflows.
- **Infrastructure Services** — access to external systems
  (`EmailSender`, `PaymentGateway`, `FileStorage`, `Clock`). They implement
  interfaces defined in the application layer.
- **Technical Services** — reusable technical functionality with no business
  logic (`PasswordHasher`, `JwtTokenGenerator`, `CacheProvider`, `Logger`).

**Dependency direction:**

```
Controller → Use Case → Repositories / Domain Services / Infrastructure Services → Database / External APIs
```

- A Use Case may invoke multiple Services.
- A Service must **never** invoke a Use Case (and never another Use Case).
- Domain knows nothing about Application; Application knows nothing about
  Infrastructure; Infrastructure depends on Application interfaces.

**Anti-pattern — avoid God Services.** Do not create generic `UserService`,
`OrderService`, or `CustomerService` classes that accumulate `createOrder`,
`cancelOrder`, `refundOrder`, `sendReceipt`, `shipOrder`… — these violate the
Single Responsibility Principle and become hard to test. Prefer many small,
focused Use Cases composed from small, focused Services. When logic is reused
across Use Cases, extract it into a Service rather than duplicating it.

**Decision tree:**

1. Initiated by a user, API, message, or scheduled job? → **Use Case**
2. Reusable across multiple Use Cases? → **Service** (otherwise keep it inside the Use Case)
3. One focused business capability? → **Domain Service**
4. Interacts with external systems? → **Infrastructure Service**

---

**Where to go next.** `CLAUDE.md` holds the enforced rules and the
"what a feature requires" / "what a component requires" checklists.
`MEMORY.md` holds this app's current state — features, components, decisions
and gotchas — and `.claude/memory/CHANGELOG.md` archives what has been trimmed
out of it.
